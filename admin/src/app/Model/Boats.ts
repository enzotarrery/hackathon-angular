export interface Boats {
  name: string,
  length: number,
  width: number,
  depth: number,
  height: number,
  maxWeigth: number,
  maxVolume: number,
  createdAt: string,
  updatedAt: string,
  publishedAt: string
}